import Layout from "../../../components/layout";

const pageTitle = "Kurulum";

export default function setup() {
  return (
    <Layout siteTitle={pageTitle}>
      <h1>{pageTitle}</h1>
    </Layout>
  );
  }